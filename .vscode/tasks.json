{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "dotrush: Build",
            "type": "shell",
            "command": "dotnet build ${workspaceFolder}/ToDo.Net.sln",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$msCompile"
            ]
        }
    ]
}